import React from 'react';
import Header from '../../Components/Header/Header';

const HomeLayouts = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default HomeLayouts;